name: Remove EKS (test)

on: delete

jobs:
  filter:
    runs-on: ubuntu-latest
    if: >-
      github.event.ref_type == 'branch' &&
      startsWith(github.event.ref, 'foo')
    steps:
      - run: |
          cat << OBJECT
          ${{ toJson(github) }}
          OBJECT
