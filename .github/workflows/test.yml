name: test
on:
  pull_request:
    types:
      - closed
    branches:
      - 'feature/ship-*'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "$EVENT"
        env:
          EVENT: ${{toJSON(github)}}
