name: TEST

on: push

env:
  TEST: test

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: test
    steps:
      - name: "Test"
        run: echo $Greeting $First_Name ${{ env.TEST }}
        env:
          First_Name: $Greeting-${TEST}
  
