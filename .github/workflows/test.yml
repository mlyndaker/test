name: Remove EKS (test)

on:
  pull_request:
    types: [closed]

jobs:
  filter:
    name: Foo
    if: >-
      github.event.pull_request.base.ref == 'main' &&
      startsWith(github.event.pull_request.head.ref, 'feature/ship-')
    runs-on: ubuntu-latest
    steps:
      - run: echo foo
      
