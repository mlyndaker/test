name: Test

on:
  pull_request:
    types: [committed]
  pull_request_review:
    types: [submitted]

jobs:
  filter:
    runs-on: ubuntu-latest
    steps:
      - run: |
          cat << OBJECT
          ${{ toJson(github) }}
          OBJECT
