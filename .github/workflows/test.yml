name: TEST

on: push

env:
  TEST: test

jobs:
  build:
    steps:
      - name: Checkout
        uses: actions/checkout@v4
          repository: shipangel/frontend-app
          ref: development
          ssh-key: ${{ secrets.SSH_KEY }}
