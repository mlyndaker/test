name: TEST

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: shipangel/frontend-app
          ref: development
          ssh-key: ${{ secrets.SSH_KEY }}
