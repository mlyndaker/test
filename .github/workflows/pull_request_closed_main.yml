name: pull_request_closed_main

on:
  pull_request:
    types:
      - closed
    branches:
      - 'main'

jobs:
  approved:
    runs-on: ubuntu-latest
    steps:
      - run: echo "$EVENT"
        env:
          EVENT: ${{toJSON(github)}}
